name: CI

on:
  pull_request:
    types: [opened]
    branches:
      - main
      
jobs:
  getflag:
    runs-on:
      labels: self-hosted

    steps:
      - name: Run Python script to generate flag value
        run: |
          python myscript.py
        id: python_script

